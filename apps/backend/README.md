# Musicum Backend

FastAPI backend for the Musicum application.

## Development

```bash
# Install dependencies
poetry install

# Run development server
poetry run uvicorn backend.main:app --reload

# Run tests
poetry run pytest
```
