export interface AppInfo {
  name: string;
  version: string;
}

export type {
  ClipListItem,
  ClipModel,
  FileListItem,
  FileMetadataModel,
  FileModel,
  PluginManifest,
} from '../plugin-api/types';
