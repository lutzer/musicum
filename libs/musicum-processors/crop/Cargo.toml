[package]
name = "crop"
version = "0.1.0"
edition = "2021"
description = "crop processor for Musicum"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
musicum-processor-sdk = { path = "../../musicum-processor-sdk" }
