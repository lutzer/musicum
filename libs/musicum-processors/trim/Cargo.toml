[package]
name = "trim"
version = "0.1.0"
edition = "2021"
description = "trim processor for Musicum"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
musicum-processor-sdk = { path = "../../musicum-processor-sdk" }
